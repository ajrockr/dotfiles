#!/bin/bash

set -e

dotfiles() {
    /usr/bin/git --git-dir="$HOME/.dotfiles/" --work-tree="$HOME" "$@"
}

MSG="${1:+$1 - }$(date '+%Y-%m-%d %H:%M')"

dotfiles add -u
dotfiles commit -m "sync: $MSG" || echo "Nothing to commit"
dotfiles push
